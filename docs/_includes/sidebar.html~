<div class="sidebar">
  <div class="container">
    <div class="sidebar-about">
      <h1>
	<img src="{{ site.url }}/public/img/salmon-grey-infinity.png" width=100%>
        <a href="{{ site.url }}">
          {{ site.title }}
        </a>
      </h1>
      <p class="lead">{{ site.tagline }}</p>
    </div>

    <nav class="sidebar-nav">

      <a class="sidebar-nav-item" href="{{ site.url }}">Home</a>

      <p><b>Latest Posts</b></p>
      {% for post in site.posts limit:5 %}
      <em><a href="{{ post.url }}">{{ post.title }}</a></em>
      {% endfor %}

      {% if page.toc %}
        <h4 style="color: white;">{{ page.title }}</h4>
        <h4 style="font-weight: normal; color: white;">Contents</h4>

      {% include toc.html html=content h_min=2 h_max=2%}
      {% endif %}

    </nav>

    <p>&copy; {{ site.time | date: '%Y' }}. All rights reserved.</p>
  </div>
</div>
